require 'spec_helper'

describe Gp::Webpay do
  it 'has a version number' do
    expect(Gp::Webpay::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
